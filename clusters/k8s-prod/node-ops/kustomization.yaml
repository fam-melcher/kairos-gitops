apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# A single, persistent, cluster-wide NodeOp (ADR 0016) — not one-per-node.
# It stays here indefinitely; a config change means a new file/object name
# (see the manifest's own header comment), not an edit to this one.
resources:
  - k3s-oidc-config.yaml
