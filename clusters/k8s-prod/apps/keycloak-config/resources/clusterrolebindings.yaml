apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: fam-melcher-k8s-admins
subjects:
  # TODO(verifier): see opencode/human/OPEN-QUESTIONS.md #2 — bare name
  # assumed; change to "/k8s-admins" together with keycloakrealmimport.yaml
  # if the realm's group-membership mapper emits full paths instead.
  - kind: Group
    name: k8s-admins
    apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: fam-melcher-k8s-users
subjects:
  - kind: Group
    name: k8s-users
    apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: view
  apiGroup: rbac.authorization.k8s.io
