apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
spec:
  source:
    # Pinned to the same tag kairos-configs's one-shot bootstrap applies
    # (configs/roles/16-argocd.yaml) — no version drift the instant
    # self-management takes over.
    targetRevision: "v3.4.5"
